import styled from "styled-components";

const Container = styled.div`

`;

const MenuToggle = () => {
    return (
        <Container>
            
        </Container>
    );
}

export default MenuToggle;
