import styled from "styled-components";

const Container = styled.div`

`;

const SidebarToggle = () => {
    return (
        <Container>
            
        </Container>
    );
}

export default SidebarToggle;
